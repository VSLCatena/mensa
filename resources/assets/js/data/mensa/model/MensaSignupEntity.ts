export default interface MensaSignupEntity {
    id?: string,
    isIntro?: boolean,
    foodPreference?: string,
    description?: string,
    allergies?: string,
    cook?: boolean,
    dishwasher?: boolean,
}