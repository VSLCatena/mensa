export default interface MensaSignupEntity {
    id?: string,
    isIntro?: boolean,
    vegetarian?: boolean,
    description?: string,
    allergies?: string,
    cook?: boolean,
    dishwasher?: boolean,
}