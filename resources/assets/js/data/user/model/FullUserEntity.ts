export default interface FullUserEntity {
    id?: string,
    name?: string,
    email?: string,
    isAdmin?: boolean,
    foodPreference?: string,
    description?: string,
    allergies?: string,
}